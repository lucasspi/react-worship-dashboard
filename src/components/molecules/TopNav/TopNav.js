import React from "react";
import { Button } from "../../atoms";

export const TopNav = () => {
  return (
    <nav className="navbar nav-top fixed-top">
      <div className="d-flex justify-content-end align-items-end w-100">
        <Button onClick={() => {}} title={"CRIAR ESCALA"} />
      </div>
    </nav>
  );
};
